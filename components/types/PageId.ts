/**
 * PageId — Re-exported from components/registry.ts
 *
 * DO NOT add IDs here manually. Add new pages to ITEMS_SPEC in registry.ts.
 * The PageId union type is derived automatically from that array.
 */
export type { PageId, NavPageId, SpecialPageId } from "../registry";
